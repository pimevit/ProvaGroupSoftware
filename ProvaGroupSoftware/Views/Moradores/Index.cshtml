@model IEnumerable<ProvaGroupSoftware.Models.Morador>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Unidade.Numero)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sobrenome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Unidade.Numero)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sobrenome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MoradorId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MoradorId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MoradorId })
        </td>
    </tr>
}

</table>
